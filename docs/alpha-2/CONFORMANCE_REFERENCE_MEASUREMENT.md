# Alpha 2 — complete reference benchmark before bounded optimization

Approved 2026-09-11. MET-PERF-005 is META-only: no product editing, import,
execution or arithmetic selection. It preserves all150 previous packet YAML,
all authority records, source locks, fixtures and historical test identities.
Catalog153 /169 YAML;13 repositories,4 planes,16 harnesses are unchanged.
Accepted META0bc353ebb9ab9dd32c6e35bddf249bc702757e4e (PR113) passed required
localhost CI and independent LOCAL exact-main. Accepted product remains
9df7dd7f2df8ac64096ef37d8df259761947d552 /tree1310cc74cc0ed39cfeb1068e998a0f78502a4be4,
127 files/327 inherited tests. Drafts12/13/14/15 are NOT accepted checkpoints.

## Retained failure; precise prerequisite replacement

CONF-PERF-003 draft15 at4662764713e32df49d5a5e8732d9f8bae5947222, activation155,
ended TIMEOUT_NOT_PASS at900.933409709 seconds during command6. The first five
suites passed170 tests with zero skips, including both formerly failing source
consumers. Backend178 /total348 were discovered, but backend completion and the
last two commands were NOT_RUN. No complete supervisor profile exists.
Log SHA256 ce2043f9ef1a9d3a8727a299e400bb673abde28db311bd0a916008d5e6e854a3.
Queued CI34519942758/job103014653560 was cancelled without a runner or artifact.
Neither the three samples inside that failed run nor its PARTIAL_DIAGNOSTIC_ONLY
prefix may become a successful reference benchmark or full baseline.

Only this new dispatch replaces the COMPLETE unoptimized eight-command baseline
prerequisite with a freshly COMPLETE isolated fixed-workload reference benchmark.
The old packet and its stricter historical gate remain immutable and unaccepted.
This is an explicit measurement-scope change, not a timeout waiver or proof that
the full product passed. Complete benchmark attribution concerns that workload
only; it must never be described as whole-suite or whole-module attribution.

## Ownership, order and exact replay contract

| ID | Role | Writes and evidence |
|---|---|---|
| MET-PERF-005 | This META coding packet / one branch / one PR | Source-only authority, data validators and tests; all27 META commands |
| CONF-PERF-004 | Future product coding packet / one branch / one PR | Same eight bounded paths, from accepted9df; scaffold first, then conditional private-_add change |
| CONF-BENCH-001 | Read-only signed reference replay, NOT a coding packet or PR | Execute the committed original-crypto scaffold from CONF-PERF-004; zero source writes, zero merge or product acceptance |

The replay packet's allowedPaths is an upper bound on its eight source inputs,
NOT edit authority: its closed execution-role record forbids every source write,
including those paths. It has no independent implementation branch, commit or
PR lifecycle; its schema-required branch label is descriptive only. All127
tracked files must be unchanged before/after execution. Results are retained
outside the checkout by the operator. Do not create an empty PR or merge the
scaffold to satisfy a fictional accepted predecessor.

The source DAG contains only already accepted prerequisites; the pre-merge
scaffold/reference/candidate sequence is a separate execution relation. Both
product and replay packets depend on MET-PERF-005, CONF-FIX-003 and CONF-FIX-005;
neither claims the other is an accepted source predecessor. CONF-PERF-004 remains
the sole source owner. Generic ownership remains unchanged; the closed adapter
removes only the72 exact diagnostics among immutable retired001/002/003,
the current004 source owner and the read-only benchmark packet (all ten pairs).
Changed packet digests, paths, recipes, roles or unrelated errors fail closed.

1. After META source gates close, create CONF-PERF-004 from accepted9df, not a
   failed draft. Implement all four exact historical bridges, fresh custody,
   v4 source proof and independent regressions within the original eight paths.
   crypto.py must still be byte-identical (SHA256
   2684b08b73d0379b8bf09e90f70839f6bccb0e947f3f999f6fa2f0e71c873035).
   Commit the scaffold; retain exact commit/tree and complete127-file inventory.
2. Nonroot-sign CONF-BENCH-001 for that exact scaffold commit and existing
   python-conformance-stdlib profile. Through ONLY the absolute trusted launcher,
   run its empty prefetch phase and single exact direct argv:
   `["python3","-m","unittest","discover","-s","tests/live_backend","-p","test_supervisor.py","-k","PerformanceArithmeticTests.test_fixed_three_sample_workload"]`.
   It must discover and pass exactly ONE named benchmark method, zero skips.
   This selected command is MEASUREMENT_ONLY, never a subset product acceptance.
3. The benchmark lives in the existing supervisor appendix. Its exact inert
   method template is pinned by this META record and is required verbatim by
   the v4 source validator. All327 accepted IDs and at least the21 draft003
   regression IDs are required in the scaffold and candidate, without implying
   that draft003 was accepted. No new file or discovery override is authorized.
4. Revalidate actual source custody before and after the benchmark. Record one
   complete profile spanning all three consecutive fixed samples, with default
   cProfile.Profile(subcalls=False,builtins=True), identical interpreter/platform,
   observer/workload/template identities, deterministic vector/result digests,
   all required calls/self/cumulative times and benchmark wall time. No module
   profiler may wrap it; reject an existing profiler and restore it on every
   exit. The former full-module observer is not used in this new dispatch.
5. Independently verify the retained signed activation, exact packet/commit/tree,
   source inventory, completed launcher result, log digest and absence of source
   overlays or skips. DATA_CHECK_ONLY validates consistency, not signatures or
   authenticity; a JSON boolean or self-reported PASS grants no execution.
   Bind all later comparison to this exact reference report and retained log.
   A complete run requires all three samples and all required functions; no
   pooling across attempts. At most one unchanged retry; retain every failure.
6. Only if that complete reference shows _add cumulative/wall>=0.50 and
   builtins.pow self/wall>=0.40 may the same CONF-PERF-004 coding run choose a
   bounded pure-integer _add candidate. Otherwise stop without algorithm choice.
   Reconstruct the reference checkpoint from accepted source plus the exact v4
   scaffold proof; never import/evaluate an inert snapshot as a reference engine.
7. Sign the candidate's exact committed head with CONF-PERF-004. Its acceptance
   remains the original EIGHT commands: all six unittest discovery roots, then
   campaign and evidence-verify. Keep the identical benchmark method in normal
   discovery, no -k, no omission, no skip, no result cache and no hidden checks.
   All independent affine/RFC8032/malformed/zero-denominator/extreme-coordinate,
   current/historical source, negative-proof and inherited tests must pass.
8. Match the candidate's fixed workload to the retained reference, including
   exact deterministic digest and observer/interpreter/platform identity.
   Median ratio<=0.85 and candidate LOCAL and LOCAL exact-main trusted elapsed
   <=750 seconds are separate requirements. Full required localhost CI, green-only
   merge and an independent LOCAL exact-main replay are mandatory. Reference
   measurements alone never authorize a merge. Retain candidate8 command
   completion and full discovered/executed test inventory for each accepted run.

## Preserved scope, timeouts and evidence states

Product scope remains eight paths,119 untouched files, all327 inherited IDs and
all first-five170 IDs/behavior, exact four historical bridge strings, existing
helper pin, append bounds and seven-stage history [110,120,127,135,141,146,151].
Use planeon.conformance-performance-delta/v4 with CONF-PERF-004 and this new
authority digest. Retain the same seven Python rows plus acyclic document suffix.
Current custody precedes historical comparison. Existing assertion AST stays
unchanged except the already fixed four operand bridges. All other public crypto,
decoder/scalar/signature, role/scope/validity/revocation contracts remain intact.

Nested420 /trusted900 /workflow15 minutes stay unchanged. No OS/root-policy,
new key, administrator prompt, dependency, toolchain, Makefile, dispatcher,
fixture, PORTING, download, hosted compute, cloud, paid API or artifact upload.
META runs all27 cumulative commands and both full replays; no new META skips.
The three already-owned predecessor history helpers and the new helper use
fresh exact raw-byte authority pins and record-matched routing tables. Successor
unwinding always happens first; identity routes grant no acceptance. Relevant
records and changed source bytes are reread on every invocation, with no parsed
authority or acceptance-result cache. All predecessor records remain immutable;
new regression tests cover fresh reads and missing/extra routes in each helper.
Product/reference never opens a warm-start source or gains telemetry/egress.

Local source, required CI, merge, LOCAL exact-main, reference measurement,
artifact, deployment, native runtime, assurance and tenant acceptance are separate.
Native Linux remains NOT_RUN_ENV_UNAVAILABLE. CONF-LIVE-003 still needs separate
accepted-checkpoint reconciliation after the repair;004/005/006 and Alpha3/4
remain waiting. Alpha2 ONGOING; effort transition NOT_DUE.
Revert an unconsumed publication as a reviewed unit; after consumption use a
bounded successor. Preserve all old source/proof/nonce/trust/tenant/failure data.
