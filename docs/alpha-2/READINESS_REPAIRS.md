# Readiness repair execution plan

Authority: `MET-REPAIR-001`, approved by the user on 2026-09-05 after a
read-only source review. This publication assigns work; it does not implement
product fixes or invalidate the packet-specific historical evidence. The
machine-readable review snapshot is
[`readiness-repairs.json`](../../architecture/readiness-repairs.json).

## Checkpoint and order

| Phase | Packet | Status at publication | Description |
|---|---|---|---|
| Alpha 2 entry | `MET-REPAIR-001` | ONGOING — source publication | Publish these boundaries, validators and 114-packet catalog |
| Foundation correction | `CON-FIX-001` | WAITING | R01/R02 contracts regression and independent status vectors |
| Alpha 1 correction | `CTRL-FIX-003` | WAITING | R03/R04/R05 freshness, portfolio and aggregation fixes |
| Alpha 2 | `CON-MODEL-001` | WAITING — corrective prerequisites | Model wire contracts; entire predecessor suite required |
| Alpha 1 integration carryover | `CTRL-INTEGRATE-001` | WAITING | R06 authenticated production overview and durable projections |
| Alpha 2 certification | `CONF-A2-001` | WAITING — independent live gates | Installed foundation, production overview and cited read-only agent |

The index is a topological order, not a claim that all predecessors have live
PASS evidence. CON-MODEL-001 may follow the two fixes without waiting for live
deployment, but CONF-A2-001 cannot certify the platform until production
integration and fresh installed-foundation evidence exist. Adding a predecessor
to the already completed CONF-A1-001 would create a history/graph cycle; its
fresh live result is instead explicitly required by the later certification.
The earlier 107-packet Phase-0 audit and 110-packet Alpha-2 publication remain
immutable snapshots. No old passing result is reinterpreted as covering these
new corrective tests.

## R01/R02 — Contracts baseline and additive releases

Reviewed destination: `mas-harness-contracts`, CON-007 main
`2146278a95344cd2a8e22596b2f315b46edffc88`.

- `scripts/generate_contracts.py:79` includes two compatibility outputs, while
  `tests/golden/test_generated_contracts.py:10` excludes those paths from its
  exact expected set. Establish a failing regression and repair the inventory
  assertion without removing compatibility artifacts.
- The same test file's fixed five-OpenAPI count conflicts with the sixth model
  API. Test equality with an independently enumerated regular JSON inventory
  and retain explicit membership of all five predecessor APIs; a missing
  baseline API, extra index claim or duplicate must fail. Merely changing 5 to
  6 or checking a lower bound is insufficient.
- `tests/runtime/test_runtime_contracts.py:221` fixes the extension list to
  CON-007. Test additive ordered unique lineage and preserved predecessor
  artifacts instead. The model packet retains no right to rewrite these tests.
- Pin the old manifest and real digest in contracts/regression-inputs. Every
  unchanged wire, runtime and compatibility entry must retain its bytes.
  Derived indexes/manifests can change only by accounted-for additions.
- Include hand-authored language-neutral aggregate-status vectors in the
  release manifest. Expected results are authored from the canonical status
  contract, not generated by the function under test. Include every evidence
  state as required and optional, selection neutrality, installation precedence,
  freshness loss and waiver behavior.
- Both CON-FIX-001 and CON-MODEL-001 run `python -m pytest tests` through their
  exact signed offline packet execution, plus deterministic generation checks.
  Shared generator changes cannot substitute only the newest test directory.

The implementation must also add negative regression cases that would fail
against the old inventory/lineage assumptions. No public state semantics,
schemas, release signing rules or dependency versions change in CON-FIX-001.

## R03/R04/R05 — Consistent tenant status

Reviewed destination: `mas-harness-control-plane`, CTRL-FIX-002 main
`97e461aa22e34002c7407555750a93b69f1b750c`.

The bounded repair owns aggregation, projection-store and HTTP/runtime clock
plumbing, independent vector intake and regression tests. It does not enable
production policies or storage.

1. Capture one clock instant per request. Every overview, plane, harness and
   portfolio read derives freshness from the admitted freshUntil and source
   state at that instant. At exact expiry the result is STALE. No new event is
   necessary, no background timer is required and original admitted facts are
   never mutated to fabricate currentness.
2. Apply the same materialization before portfolio state filtering and paging.
   Test a known source outage, multiple unavailable sources, recovery of only
   one source, full recovery after expiry, detail/list agreement and stable
   pagination. Recovery does not extend a freshness window.
3. Consume the exact CON-FIX-001 vector file and release digest through
   contracts/status-regression. Apply every independent expected case to the
   TypeScript implementation. Optional FAIL/MISSING/COLLECTING/STALE or
   unavailable evidence degrades an otherwise ready harness; higher-precedence
   required failures, revoked installation or freshness loss still win.
4. Regression descriptor: typecheck, production build, the complete Vitest
   tests directory, predecessor overview browser acceptance and zero-bill.
   Preserve tenant-derived context, deny policies, separate operator policy,
   audit-before-response, neutral unselected harnesses and all existing APIs.

## R06 — Production composition is explicit work

CTRL-007 intentionally serves labelled synthetic previews and denies real
policies until an approved adapter exists. That historical boundary was honest;
the missing follow-on owner is now CTRL-INTEGRATE-001.

The integration owns production page/API composition, durable projection and
audit ports, an additive migration, tests and rollout documentation. It must:

- Keep preview fixtures out of production imports/composition and never fall
  back to a fixture organization on authentication, policy or database failure.
- Use existing opaque server sessions and existing purpose-bound policy ports;
  never grant authority through a role string, tenant header, browser storage,
  environment default or a new signing/authentication primitive.
- Persist ordered per-organization cursors, exact event replay, last verified
  projections and operator audit atomically. Exercise crash before/after commit,
  process restart, audit refusal, RLS denial and concurrent tenant operations.
- Render real authenticated application pages through the local API/projection
  boundary. Tests must not substitute only static HTML inspection. Cover empty,
  missing, stale, unavailable, forbidden and authorized-operator views.
- Use only locked dependencies and separately preinstalled, disposable local
  PostgreSQL capacity. Do not borrow a host database, download an engine, grant
  BYPASSRLS or broaden the signed offline network boundary. If the required
  adapter, executable/cache or isolated database is absent, stop or report
  NOT_RUN_ENV_UNAVAILABLE as appropriate; it is not production acceptance.

The integration packet has no liveCampaignExecution. Actual tenant execution
remains manual, post-merge, independently signed and owned by CONF-A2-001. Its
live evidence must include fresh CONF-A1-001 installed-foundation results and
the authenticated browser/API/PostgreSQL journey. Final tenant acceptance is
still an independent tenant signature, not a campaign-produced PASS.

## Verification and host handoff

The meta validator checks all four new packet identities, predecessor gates,
exact product path boundaries, cumulative command lists, R01–R06 ownership,
historical counts and authority-only evidence. Negative vectors remove each
gate, widen paths, reduce tests, fabricate completion and substitute fixture
evidence. General schema, acyclic graph, shared-path ownership, source-reuse,
zero-bill and complete meta regressions remain mandatory.

No acceptance was run by the source review itself. For each corrective packet:

1. Prepare exactly that packet on its own branch; review allowed paths and
   predecessor source locks. Do not edit a product repository in the meta PR.
2. Obtain the operator-installed, signed exact packet/launcher authority and
   fresh isolation preflight on the pre-existing localhost runner. Retain every
   warm-root and credential/socket denial. Never reuse an old packet pin or
   fabricate a key, signature, preflight or cache digest.
3. Run only the declared argv through the single isolated process tree, then
   retain local result, required PR check, merge and exact-main replay separately.
4. Update a later evidence checkpoint with real results. The review record's
   WAITING_IMPLEMENTATION fields remain its publication-time snapshot, not a
   mutable dashboard. Do not rewrite historical evidence to turn it green.

The current model choice does not change packet authority. At each completed
phase, report the phase/ID/status backlog and an effort recommendation; routine
bounded fixes suit high effort, while cross-contract/integration acceptance
deserves extra-high review. No model change is performed automatically.

## Rollback

Before consumers execute, revert the meta authority as one compatibility unit.
After consumption, supersede through another packet rather than rewriting
source history. Product rollback preserves public predecessor contracts,
immutable vectors, admitted tenant projections and audit history; database
rollback must never delete tenant data or undo additive schema destructively.
