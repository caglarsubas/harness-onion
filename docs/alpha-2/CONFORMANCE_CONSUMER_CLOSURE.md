# Alpha 2 — transitive historical-consumer closure

Approved 2026-09-10. MET-PERF-004 is a source-only META publication, followed
by separate product packet CONF-PERF-003. Preserve all148 existing packet YAML
and all earlier authority records. Catalog150 /166 YAML;13 repos,4 planes,
16 harnesses unchanged. Accepted meta a0384bd4c6e2559d897ad3df637144cf1294aaff
(PR112) passed local, required localhost CI and independent LOCAL exact-main.
Accepted product9df7dd7f2df8ac64096ef37d8df259761947d552,tree1310cc74cc0ed39cfeb1068e998a0f78502a4be4,
127 files/327 tests remains the only starting checkpoint. Drafts12/13/14 are
unaccepted; never substitute their source as an accepted baseline.

## Observed gap and retained evidence

CONF-PERF-002 draft14 at a1871274e4243d9166d528d8b75d754298d56852 used signed
activation150. Full recipe stopped at command5 after230.821783667 seconds:
170 executed,168 passed,2 failed,zero skipped. Backend175 and the final two
campaign commands were NOT_RUN; profile and workload NOT_RUN. Log SHA256
87ee7b0c15fc154fd4299e3f4c22b0b3b7412e2d1fcd5843480bced68eb0a51b.
CI34493434012/job102925714045 was CANCELLED_QUEUED_NO_RUNNER_NOT_PASS,
runner_id0,zero artifacts. Preserve earlier143/144/003 failure logs and partial
samples unchanged. Attribution is NOT_YET_MEASURED; no algorithm selected.

The prior fixed scalar bridge changed its file bytes, but two additional
successor-test methods still required accepted historical scalar bytes. They
were outside the prior closed region grant. This is a source-accounting
contradiction, not demonstrated crypto or runtime failure. No unchanged retry,
checksum weakening, fixture substitution or test bypass is authorized.

## Same eight paths; exactly two additional fixed methods

All six original region/interface/append bounds remain. The original scalar
bridge and original106-history bridge retain their exact after strings. Only
these two further methods in tests/platform/linux_baseline/test_successor_inventory.py
are added to the fixed template map:

| Method | Exact additional transformation | Invariants retained |
|---|---|---|
| SuccessorInventoryTests.test_current_test_guard_not_exempt | Call HELPER.performance_current(ROOT) first; change only current-disk hash operand to historical_sources[path] | e1491e4407ff6d221871b45bbd775beb28afba11d418ae001847a512bb4b6fe6 expected hash; all seven-stage rejection checks |
| SuccessorInventoryTests.test_exact_scalar_test_patch | Call the same current-source validator first; change only current-disk comparison operand to historical_sources[RECORD["change"]["path"]] | unchanged corrected_test, three hunks,30 IDs, reversed reconstruction, every malformed-input refusal |

No newly added product path or broad method edit. Other119 files unchanged.
All327 inherited IDs and behavioral parameters remain; all first-five170 IDs
unchanged. Expected checksums/fixtures/check_edit/corrected_test remain intact.
The fixed templates must all be present for the original-crypto baseline too.

## Transitive consumer census and v3 source proof

Pin every accepted product Python file to the127-file checkpoint by hash,
size and Git blob. The inert census is STATIC_SOURCE_CENSUS_ONLY; parse AST
only, never import, evaluate, compile or execute snapshots. Enumerate all
read_bytes/read_text/regular_bytes and inventory/proof consumer call sites,
with source path, scope, line and expression. Check all327 static test IDs
against accepted checkpoint and all eight region-source bytes against census.
This reproducible inventory supplements review; it is not a semantic proof
that all possible indirect behavior is safe or a substitute for full execution.

Consumer chain: original91 Linux guard -> scalar exact-edit -> successor
current-scalar-hash AND exact-scalar-patch AND original-history -> cumulative106
stage inventory -> backend110 -> supervisor120 -> custody279 -> credential305
-> accepted127/327 -> fresh six-root discovery. In tests, prove each of the
four before methods contains its historical disk read and each exact after
method validates fresh current bytes first and has no direct historical disk
operand. Enumerate the entire source census so unreviewed omissions fail.

Use planeon.conformance-performance-delta/v3, packetId CONF-PERF-003 and this
new pinned authority digest. Keep the same closed proof fields as v2 and seven
Python rows; no document self-digest. Exact original prefix + bounded suffix
+ canonical proof fence; current source custody/hashes before inert history.
All unchanged behavioral bytes, helper pin and existing historical proof chain
remain mandatory. Four exact fixed regions are the only accounting assertion
operand alternatives; other inherited accounting assertions keep their AST.
Independently reseal mutations to each new bridge's operands, expected hash,
loop bound, ID count, validation call or extra statement; fail the intended
region guard, not a stale after hash or document JSON. Keep outside-region,
forged-before, helper-pin, symlink/hardlink/mode and collection-override refusals.

## Unchanged performance, safety and execution gates

Retain cProfile.Profile(subcalls=False, builtins=True), default timer, complete
supervisor module and normal/exception cleanup; restore prior profiler state.
Required _add/_scalar_mult/sign/verify/builtins.pow counts/self/cumulative times
and module wall time. No thread, signal hook or production monkeypatch.
Optional cumulative getstats snapshot is PARTIAL_DIAGNOSTIC_ONLY,complete=false;
never create_stats mid-run, disable/restart mid-run or promote partial evidence.
Within-invocation parse-once data allowed; no cross-call acceptance/signature
cache, fixture memoization, hidden/skipped tests or omitted history.

Run all eight original product commands with unchanged crypto for a COMPLETE
baseline. Same full tests/observer/workload before and after. Fixed seed1,
byte0x61 messages0/1/32/1024,two public_key/sign/valid/tampered repetitions,
three consecutive samples, identical deterministic result digests and metadata.
Only complete material _add/pow attribution permits a bounded private _add
change. Keep independent affine/RFC8032, malformed, zero-denominator, extreme,
unreduced and negative-coordinate tests. No public crypto change.
Matched median ratio<=0.85; candidate local and LOCAL exact-main<=750 seconds.
Nested420/trusted900/workflow15 minutes unchanged. If the complete baseline
is unavailable or attribution insufficient, retain failure and stop: no subset,
timeout increase or algorithm selection. No wall-clock correctness assertion.

META runs all26 cumulative commands and both full replays, preserving all
existing nested-isolation skips and adding none. Generic ownership checker
unchanged; closed adapter removes exactly20 diagnostics for pinned001/002/003
pair overlaps6/6/8. Every other diagnostic survives. No older YAML rewrite,
accepted-prerequisite fiction, broader same-repository overlap exemption or
warm-source access. No new dependency, fixture, toolchain, root policy, key,
administrator prompt, hosted compute, cloud, paid API, download or artifact upload.

Local head, required localhost CI, green-only merge and independent LOCAL
exact-main are separate. At most one unchanged retry; failures retained.
Source proof is SOURCE_DELTA_ONLY, never native/runtime/assurance/tenant acceptance.
Native Linux remains NOT_RUN_ENV_UNAVAILABLE. After performance repair, separate
accepted-checkpoint reconciliation still precedes CONF-LIVE-003 draft12;
CONF-LIVE-004/005/006 and Alpha3/4 remain waiting. Alpha2 ONGOING;
effort transition NOT_DUE. Roll back an unconsumed publication as a reviewed
unit; after consumption use a bounded successor, preserving all prior evidence.
