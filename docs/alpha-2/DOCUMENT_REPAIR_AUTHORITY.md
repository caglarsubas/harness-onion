# Alpha 2 — exact document repair authority

Owner: MET-PERF-012. Status: ONGOING_PUBLICATION. This META packet publishes two
conditional successors. It runs no product code, test, diagnostic or benchmark.
Authoritative baseline: META abb67fde635dd45c8c3f6fbb3576a0a391300c91 (PR126).
MET-PERF-011 completed LOCAL, required localhost CI, protected merge and independent
LOCAL exact-main. Its old failures and consumed allowances remain immutable.

## Exact scope and sequence

1. Finish this META publication's source gates first: LOCAL, CI, merge, exact-main.
2. Separately implement CONF-PERF-006 from accepted product
   092fcf475c6f3ebd455e3c354cddb7664ea1f900, not an unaccepted draft.
3. Freeze the candidate commit, tree, all135 file pins, inherited1277 and appended32
   test identities, source functions and toolchain before any comparison.
4. Separately activate CONF-BENCH-002 with the signed comparison manifest; reserve
   every ordinal before activation and run only BASELINE,CANDIDATE,CANDIDATE,BASELINE.
5. Require complete practical-gain/noise evidence, then full eight-command product
   LOCAL, required localhost CI, protected merge and independent LOCAL exact-main.
   A product source change invalidates the prior comparison, not its consumed budget.
6. PR18 completion integration remains a distinct later authority; no rebase,
   cherry-pick, merge, closure or budget reset of drafts13/14/15/18 is authorized here.

The three product paths are the document function in
src/harness_conformance/live_mutation_admission.py, append-only independent tests
in tests/live_backend/test_mutation_admission.py and an appended SOURCE_DELTA_ONLY
proof in docs/live-backend/proxy.md. All132 other files remain byte/mode/size/blob
identical. The32 required methods yield1309 total and1139 backend identities.
No old test, proof, inventory helper, source consumer or fixture may be rewritten.
Static stage3 membership analysis shows no consumer edits required; a contrary
full-suite result stops the packet rather than broadening its scope.

Only exact built-in bytes with exact built-in int maximum use the candidate branch.
Keep size check, canonical parsing, byte equality, bounded traversal, encoded-size
check and fresh detached value. Every other input retains the original fallback
verbatim. No shared canonical/crypto/guard, public API or error-policy change.
The frozen function is data only; do not execute stored historical source.

## Bound artifacts and measurement

The closed architecture/document-repair-execution.json and its source-authority
record bind source-scope.json, semantic-oracles.json, workload.json and benchmark.py.txt
under architecture/document-repair-execution-inputs/. The copied review manifests
retain their historical unexecuted labels; the new authority supplies conditional
publication status, not measured results. source-scope includes all135 baseline pins
and1277 test identities. semantic-oracles freezes32 independent cases and expected
errors/values; comparison with the candidate alone is insufficient.

The inert benchmark driver remains byte-identical to the reviewed artifact. Static
AST inspection confirms its Unicode literals match the frozen ten payloads, and a
regression rejects double-escaped substitutions. Workload, sample count, thresholds,
authority and budgets are unchanged. No benchmark ran here.

Each of4 runs uses64 iterations per case, one warmup plus seven measured samples:
5120 calls/run, including identical equality and deadline overhead. Maximum120s/run
inside the unchanged trusted900-second bound. No profiling, runtime downloads,
source overlays, monkeypatching, hidden warmups or extra retries.

All14 candidate measured samples together must have wall median ratio<=0.90 and
thread-CPU median ratio<=0.85 versus all14 baseline samples. Each run's relative
MAD must be<=0.15 for both clocks; each measured wall sample>=0.05s. The two baseline
run medians' max/min ratio<=1.20 for both clocks. Every source/host/observer/custody
check must pass. Missing or interrupted measurements mean INCONCLUSIVE/NO_GO;
never pool partial success. This helper benchmark proves neither service-wide nor
PR18 speedup and cannot replace full acceptance or close C1-C7.

## META acceptance and historical boundaries

Exactly173 specifications:170 immutable predecessor YAML plus this META packet and
two conditional successors. All accepted authority JSON/release/reuse/toolchain
locks and old evidence remain immutable. The predecessor's planning-only assertions
run on a closed170-packet historical projection only after all three new packet
digests and the complete live catalog are checked. Unknown or changed successors
must fail. Current-first metadata recipes are exact, reversible and fresh-read;
no arbitrary exemption, cached acceptance or historical source execution.

Run all39 predecessor commands plus the new validator,40 total; both full suites
and10 inherited isolation skips per suite remain. Freeze exact added and expanded
case counts before signing. New META caps: LOCAL2, CI2, LOCAL_EXACT_MAIN1. Future
product caps: LOCAL2, CI2, LOCAL_EXACT_MAIN1; measurement4 total,0 retries. Exclusive
durable reservations precede signatures/activation. Every failure consumes its
ordinal. No old allowance reset or transfer; no extra diagnostics.

LOCAL/main750s, nested420s, trusted900s and workflow15min limits are unchanged.
Use only the existing signed localhost launcher. No root/key/admin/power changes,
warm-source access, dependency downloads, hosted/cloud/API spending or policy bypass.
Source, CI, merge, artifact, native, deployment, runtime and tenant acceptance remain
separate evidence. Publication can finish without claiming any product qualification.

## Current roadmap

| Phase | ID | Status | Description |
|---|---|---|---|
| Phase0/Alpha1 | Foundations | DONE_RECORDED | Historical source/offline foundations |
| Alpha2 | MET-ADOPT-002 | DONE_RECORDED | All16 harness/paper/OSS direction |
| Alpha2 | MET-PERF-011 | DONE_SOURCE_GATES | PR126, acceptedabb67fd; bounded plan |
| Alpha2 | MET-PERF-012 | ONGOING_PUBLICATION | This exact conditional authority |
| Alpha2 | CONF-PERF-006 | WAITING_META_SOURCE_GATES | Separate three-path repair |
| Alpha2 | CONF-BENCH-002 | WAITING_META_AND_EXACT_CANDIDATE | Separate4-run comparison |
| Alpha2 | CONF-FIX-007/PR18 | BLOCKED_LOCAL_BUDGET_EXHAUSTED | Completion integration still required |
| Alpha2 | CONF-LIVE-004/005/006 | WAITING | Native probes, packaging and campaign integration |
| Alpha2 | CONF-A2-001 | WAITING | Qualified integrated read-only profile |
| Alpha3/4 | Later roadmap | WAITING | Governed actions and enterprise qualification |

The [all16 harness/paper/OSS map](HARNESS_PAPER_REPOSITORY_MAP.md), four planes and13
accountable repositories remain unchanged. Require **at least one qualified baseline
for every released harness capability** at the first enterprise release. Linux
Kubernetes/OpenShift is production; macOS supports development. Alpha2 remains open;
model-effort transition NOT_DUE.
