# Successor checkpoint compatibility — MET-REPAIR-016 / CONF-FIX-006

Approved 2026-09-11. This publication is META-only. Catalog155 /171 YAML;
thirteen repositories, four planes and sixteen harnesses remain unchanged.
All153 predecessor packet YAML and prior authority records stay byte-identical.

## Diagnosis and accepted checkpoints

Accepted META2e882d0a4e8288c124bce0a7f9fef315d78c5147 (PR114/MET-PERF-005).
Accepted conformance b7586c4b8315dc92051f0b5445b2a9a0204a97bf (PR16/CONF-PERF-004),
tree ef7e5afc04c31651bd3f29acc03f59c6c901c130:127 files/354 tests. Its signed
reference benchmark, full local and required localhost CI34554588009, merge and
independent LOCAL exact-main all closed. Do not repeat arithmetic selection or
mistake failed draft12 for an accepted predecessor. Native results remain separate.

CONF-LIVE-003 draft12 e5cecc64b237f3f06f17e1178258aae017bc05ea has135 files.
Activation166 full-recipe replay ran425 tests:424 passed,1 failed,zero skips;
143.499375seconds. Log88adb4db5c35bc08ef67b088875b7ec61d0839c625493d294032a011ff2f5de9.
The first five commands passed; command6 failed; commands7/8 were NOT_RUN.
The accepted performance test asserts127 actual files. Its later exact test-map
comparison also excludes approved successor test modules (source inspection;
not reached in the failed replay). Other425-test results do not make it PASS.
Draft headf1c2fe3 only documented that failure. CI34557185507 was cancelled queued,
runner0,zero artifacts. Preserve all failed evidence and all71 passing draft tests.

## Minimal product ownership: two existing files

CONF-FIX-006 starts from acceptedb7586c4, not draft12. It may edit only:

1. tests/live_backend/test_supervisor.py: replace exactly the named
   PerformanceSourceProofTests.test_exact_checkpoint_scope_and_all_fresh_test_roots
   method with this authority's inert exact specification, then append bounded
   independent correction tests/helpers. Every other accepted byte and all354
   test identities remain; benchmark method and all arithmetic bytes are unchanged.
2. docs/live-backend/linux-boundary.md: retain every byte before the original
   final performance-proof fence, add one independent corrective binding in the
   document suffix, and rebind only the test row/new IDs/suffix in the legacy
   v4 source projection. No helper, importer pin, runtime, fixture, Makefile,
   workflow, toolchain, dependency, PORTING or warm-start change is permitted.

The v4 projection is a compatibility representation originating in CONF-PERF-004,
not authority or a claim that004 implemented this correction. Its original exact
bytes remain immutable in acceptedb7586c4 and this META publication's complete
inert product-before snapshot. Do not relabel or overwrite that original snapshot.
The current projection is explicitly REBOUND_COMPATIBILITY_VIEW; current mutation
ownership and before/after evidence belong solely to CONF-FIX-006 and its separate
canonical corrective binding. Historical proofs embedded before the final v4
fence remain byte-identical. No new public wire/signature schema is introduced.

The corrective binding has exactly schemaVersion, evidenceClass, packetId,
authorityDigest, baseCommit, beforeTestSha256, afterTestSha256,
originalProofSha256, replacementSha256 and addedTestIds. It uses
planeon.internal.successor-checkpoint-correction/v1 and SOURCE_DELTA_ONLY;
no self/document hash, credential, native PASS or tenant acceptance. Its one
harness-successor-checkpoint-correction fence precedes the reissued final v4
fence. The projection's only deltas are the supervisor append/afterSha256,
newTestIds and documentSuffix. All beforeSources, original authority/base/packet
identity, other six Python rows and all327 historical identities stay unchanged.
The new validator independently reconstructs and binds both files as data.

## Exact successor closure, not a minimum-only test

The corrected test first validates actual source custody, complete ordered stage
and the exact final launcher hook. It then accepts precisely stages2–6 with
127/135/141/146/151 paths. Historical327 methods, all27 accepted performance
methods, newly appended correction methods and tests from exactly the approved
successor paths must equal fresh collection over all six roots. Missing, partial,
out-of-order or unknown paths/modules/IDs must refuse. No filtered historical
inventory may stand in for actual current files, and no scalar is merely removed.

Product regressions must independently cover all five stages, omitted/extra or
partial paths, missing/duplicate/shadowed methods and collection overrides,
tampered final-hook proof, altered current predecessor bytes, resealed legacy
projection/correction binding, runtime/benchmark scope exclusion and fresh
current-before-historical checks. Synthetic source inventories are explicitly
SOURCE_DATA_ONLY; they cannot prove runtime, native Linux or tenant acceptance.
No inert snapshot/template may be imported, compiled or executed as a fixture.

## Sequencing and evidence

MET-REPAIR-016 owns only this publication: all28 META commands, including the
new validator and both complete test replays, then required localhost CI,
green-only merge and independent LOCAL exact-main. Historical metadata bridges
are exact reversible replacements with fresh byte pins, never acceptance caches.
Only eight new immutable overlap diagnostics (three retired performance drafts
and the read-only benchmark versus006, two paths each) are closed by the new
dispatch adapter. Existing72 diagnostics remain independently checked.

CONF-FIX-006 is the next separate coding branch/PR. Run the unchanged full8
recipe, all354 prior tests plus correction tests, zero product skips, required
localhost CI, green-only merge and independent LOCAL exact-main. Then reconcile
draft12 with this newly accepted checkpoint; do not modify its eight files here.
Historical compatibility does not require another benchmark-only or arithmetic
optimization campaign. The full unchanged benchmark still runs with normal tests.

Preserve nested420/trusted900/workflow15-minute bounds and bounded fix/retry.
No OS/root policy, admin prompt, new key, download, hosted compute, cloud, paid
API, billable resource, artifact upload or live execution. Source/CI/merge/main,
artifact/deployment/native/runtime/assurance/tenant acceptance remain distinct.

| Phase | ID | Status at publication | Description |
|---|---|---|---|
| Phase0 / Alpha1 | Foundations | DONE_RECORDED | Historical source gates |
| Alpha2 | MET-PERF-005 / CONF-PERF-004 | DONE_SOURCE_GATES_RECORDED | Accepted authority and performance repair |
| Alpha2 | MET-REPAIR-016 | ONGOING | Two-path corrective authority |
| Alpha2 | CONF-FIX-006 | WAITING | Exact inventory method and proof binding correction |
| Alpha2 | CONF-LIVE-003 / draft12 | BLOCKED_PREDECESSOR | Then unfinished native inspector/broker/API work |
| Alpha2 | CONF-LIVE-004/005/006 | WAITING | Probes, packaging, integration |
| Alpha2 | Native AMD64/ARM64 | NOT_RUN_ENV_UNAVAILABLE | Independent installed qualification |
| Alpha3 / Alpha4 | Governed actions / enterprise matrix | WAITING | No phase promotion |

Before consumption revert a publication as one reviewed unit; afterwards use
a bounded successor. Preserve original snapshots, signed history, failed logs,
replay/nonce stores and tenant data. Alpha2 remains open; effort transition NOT_DUE.
