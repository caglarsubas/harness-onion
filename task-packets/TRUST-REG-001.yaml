id: TRUST-REG-001
repository: mas-harness-trust-plane
branch: codex/trust-reg-001-agentops-registry
objective: Implement module, agent, and release registry lifecycles with certification axes, promotion, deprecation, revocation, and signed admission.
predecessors: [TRUST-GOV-001, MET-005]
allowedPaths: ["services/registry-service/", "src/planeon_trust/registry/", "migrations/registry/", "tests/registry/", "fixtures/registry/", "deploy/helm/registry-service/", "ci/targets/trust-reg-001.json"]
warmSourceAccess: PROHIBITED_DURING_IMPLEMENTATION
sourceReuse:
  - {repository: "git@github.com:caglarsubas/data-source-harness.git", commit: "858281f4b845ffacfe05cdb2c40a402c237d4c54", reuseMode: REFERENCE_ONLY, paths: ["src/data_source_harness/promotion.py","src/data_source_harness/conformance.py"], strategy: "historical reference-only exact-blob provenance for independent clean-room parity; implementation access, copying, adaptation, translation, and derivative reuse are forbidden; direct reuse requires a future packet revision after porting admission is enabled"}
contracts: ["Consumes ModuleRelease, BundleRelease, evidence, approval, and release-lock contracts v1alpha1", "Produces AgentOps registry API v1"]
deliverables: ["Immutable versions, separate certification axes, production gates requiring fresh PASS for every required control, signed release admission, deprecation/revocation, dependency impact, and audit events."]
excluded: ["Treating green CI or a waiver as production-control satisfaction, mutating published versions, or automatic promotion."]
prefetchCommands: []
offlineAcceptanceCommands: [["make","registry-contract"],["make","promotion-vectors"],["make","security"]]
offlineExecution: {"wrapperArgv":["./ci/verify-offline.sh"],"packetPathEnvironment":"HARNESS_TASK_PACKET","packetPathMode":"HASH_PINNED_READ_ONCE_NO_CHILD_PATH","commandTransport":"ARGV_ARRAY_V1","isolation":"OS_ENFORCED_DENY_ALL_OUTBOUND","sessionScope":"SINGLE_PROCESS_TREE","prefetchOutsideSession":false,"offlineEnvironment":{"UV_OFFLINE":"1","UV_FROZEN":"1","UV_NO_SYNC":"1"}}
expectedEvidence: ["Lifecycle and promotion state coverage, including denial when any required control is non-PASS despite a valid waiver.", "Missing-axis, forged signature, revoked dependency, stale evidence, waiver-as-PASS, and cross-tenant denial vectors."]
rollback: Revert registry code while preserving immutable release, revocation, certification, and audit records.
