id: CTRL-INTEGRATE-001
repository: mas-harness-control-plane
branch: codex/ctrl-integrate-001-production-overview
objective: Close R06 by implementing explicit production composition of authenticated status pages, approved policy ports and durable tenant projections without treating fixtures as live acceptance.
predecessors: [CTRL-FIX-003]
allowedPaths: ["apps/control-web/src/lib/harness-status/", "apps/control-web/src/app/overview/", "apps/control-web/src/app/planes/", "apps/control-web/src/app/harnesses/", "apps/control-web/src/app/organizations/", "apps/control-web/src/app/api/v1alpha1/overview/", "apps/control-web/src/app/api/v1alpha1/planes/", "apps/control-web/src/app/api/v1alpha1/harnesses/", "apps/control-web/src/app/api/v1alpha1/organizations/", "packages/db/status-projections/", "packages/db/migrations/harness-status/002_durable_adapters.sql", "contracts/status-integration/", "tests/harness-status/", "e2e/harness-status-production.spec.ts", "ci/targets/ctrl-integrate-001.json", "docs/harness-overview.md", "docs/security/status-production-integration.md"]
warmSourceAccess: PROHIBITED_DURING_IMPLEMENTATION
sourceReuse: []
contracts: ["Consumes existing opaque-session tenant identity and separately authorized operator policy contracts unchanged", "Consumes immutable status, event and source-admission bindings", "Produces durable local projection adapters and authenticated production pages", "CONF-A2-001 owns separate signed live integration evidence; this packet never activates tenant infrastructure"]
deliverables: ["Separate explicit synthetic preview composition from production composition; production overview, plane, harness and organization pages use authenticated local projections and never seed or fall back to fixture tenants.", "Connect the existing request authenticator and purpose-bound policy interfaces through typed fail-closed composition; missing identity, policy, trust or storage is unavailable/denied, never a role/header/environment allow shortcut. Changes to identity or policy contracts require separate authority.", "Implement a transactional durable adapter for projections, per-organization source cursors, event deduplication and operator audit using existing locked PostgreSQL dependencies; forced RLS, transaction-local tenant context and separately scoped operator reads remain enforced. No tenant table gets BYPASSRLS or unrestricted cross-organization grants.", "Use additive migration only; preserve prior projection/audit data and make crash-before-commit, crash-after-commit and exact replay atomic and deterministic. Pin consumed schema/release digests in contracts/status-integration.", "Run real application route/page tests with explicitly injected synthetic sessions and durable test storage, not HTML-only previews; reject cross-tenant access, stale sessions, forged sources and failed audit writes, and cover empty/unavailable states, restart/replay and clock/outage consistency.", "Add a disposable PostgreSQL integration harness using only externally preinstalled local binaries and authorized isolated storage; if absent, record NOT_RUN_ENV_UNAVAILABLE instead of a tautological pass, substituted in-memory database pass or shared-host mutation.", "Document the production-adapter selection and rollout/rollback prerequisites and hand off exact image, migration and test identifiers to CONF-A2-001 without claiming deployment or final tenant acceptance."]
excluded: ["New public identity, policy or status semantics, tenant infrastructure activation, shared database mutation, destructive migration/down-migration, new dependencies or online engine downloads, warm sources, cloud/API bills, new telemetry, bootstrap/Makefile/dispatcher/workflow/PORTING edits and final tenant acceptance signing."]
prefetchCommands: [["make","prefetch"]]
offlineAcceptanceCommands: [["make","status-integration-regression"],["make","status-integration-e2e"],["make","status-integration-postgres"],["make","zero-bill"]]
offlineExecution: {"wrapperArgv":["./ci/verify-offline.sh"],"packetPathEnvironment":"HARNESS_TASK_PACKET","packetPathMode":"HASH_PINNED_READ_ONCE_NO_CHILD_PATH","commandTransport":"ARGV_ARRAY_V1","isolation":"OS_ENFORCED_DENY_ALL_OUTBOUND","sessionScope":"SINGLE_PROCESS_TREE","prefetchOutsideSession":false,"offlineEnvironment":{"UV_OFFLINE":"1","UV_FROZEN":"1","UV_NO_SYNC":"1"}}
expectedEvidence: ["Complete predecessor Vitest suite, typecheck/build and production-route browser tests pass under deny-all outbound; preview and production modes cannot be confused.", "Fresh authenticated browser-to-API-to-durable-storage evidence includes restart, transaction replay, cross-tenant RLS denial and independently authorized operator audit; PostgreSQL absence is NOT_RUN_ENV_UNAVAILABLE and blocks production readiness.", "No new session/policy contract or RLS bypass is introduced; unresolved adapter or toolchain decisions stop this packet for revised authority.", "Source/offline completion cannot satisfy artifact, deployment, runtime, assurance or tenant acceptance; actual installed behavior remains a separately signed CONF-A2-001 live gate."]
rollback: Revert to the last admitted application release and retain additive database state and audit history; never delete tenant data, downgrade schema destructively or silently enable synthetic production data.
