id: CON-001
repository: mas-harness-contracts
branch: codex/con-001-contracts-bootstrap
objective: Bootstrap a reproducible Python 3.12 contracts package and CLI that builds and tests completely offline.
predecessors: [MET-002, MET-003]
allowedPaths: ["src/planeon_harness_contracts/", "tests/", "pyproject.toml", "uv.lock", "Makefile", "README.md", ".gitignore", "LICENSE", "NOTICE", "SECURITY.md", "CONTRIBUTING.md", "AGENTS.md", "PORTING.yaml", "ci/", ".github/workflows/verify.yml"]
warmSourceAccess: PROHIBITED_DURING_IMPLEMENTATION
sourceReuse: []
contracts: ["Consumes zero-bill policy v1", "Produces planeon-harness-contracts package shell", "Produces harnessctl command shell with closed packet-owned command descriptors", "Produces closed packet-local Make target descriptor dispatch v1", "Produces inert destination PORTING ledger v1 with zero current copy authorizations"]
deliverables: ["Typed src-layout package, empty registry/validation APIs, frozen dependency lock, reproducible wheel/sdist, common Make targets, and a harnessctl dispatcher that loads only closed src/planeon_harness_contracts/commands/*.json registrations.", "Bootstrap Makefile delegates only to ci/run_make_target.py, which validates closed ci/targets/<packet-id>.json descriptors, exact target and variable bindings, cumulative lexical handler order, and direct argv without shell strings; later packets never edit Makefile.", "PORTING.yaml contains only the closed NO_AUTHORIZATION bootstrap sentinel and cannot name a source path, mapping, copied file, or applied port."]
excluded: ["Domain schemas, compiler behavior, network calls, or deployable services."]
prefetchCommands: [["make","prefetch"]]
offlineAcceptanceCommands: [["uv","build","--offline","--frozen","--no-sync"],["make","zero-bill"]]
offlineExecution: {"wrapperArgv":["./ci/verify-offline.sh"],"packetPathEnvironment":"HARNESS_TASK_PACKET","packetPathMode":"HASH_PINNED_READ_ONCE_NO_CHILD_PATH","commandTransport":"ARGV_ARRAY_V1","isolation":"OS_ENFORCED_DENY_ALL_OUTBOUND","sessionScope":"SINGLE_PROCESS_TREE","prefetchOutsideSession":false,"offlineEnvironment":{"UV_OFFLINE":"1","UV_FROZEN":"1","UV_NO_SYNC":"1"}}
expectedEvidence: ["Wheel and sdist SHA-256 digests.", "Offline test/type-check report with no undeclared network access.", "Negative Make-dispatch vectors reject missing, unknown, or duplicate targets; owner or filename mismatch; undeclared variables; shell transport; and zero or ambiguous applicable handlers.", "Negative harnessctl registry vectors reject unknown or duplicate commands, owner/descriptor mismatch, handler paths outside packet authority, and predecessor-closure bypass.", "PORTING bootstrap validation proves zero authorized source mappings and rejects any copy claim without a future approved PORT_CANDIDATE packet."]
rollback: Delete or revert only the unconsumed repository scaffold.
