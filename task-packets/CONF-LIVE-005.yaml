id: "CONF-LIVE-005"
repository: "mas-harness-conformance-labs"
branch: "codex/conf-live-005-release-candidate"
objective: "Reproducible packaging and operator handoff under the approved trusted backend roadmap; source coding only."
predecessors: ["CONF-LIVE-004"]
allowedPaths: ["ci/build_live_backend.py","src/harness_conformance/live_backend_package.py","tests/live_backend/test_package.py","fixtures/live-backend/package-vectors.json","docs/live-backend/operator-handoff.md"]
warmSourceAccess: "PROHIBITED_DURING_IMPLEMENTATION"
sourceReuse: []
contracts: ["Consumes docs/alpha-2/LIVE_BACKEND_READINESS.md and architecture/live-backend-roadmap.json from the exact merged MET-LIVE-001 authority, the existing trusted live-runner contract and unchanged CONF-LINUX-001/CON-007 wire contracts. Pin exact merged predecessor SHA and complete source inventory before edits.","Source-only enablement before the native gate is limited to these six packets. No installation or live execution occurs in a coding run. Missing authority, supported OS backend or native capacity fails closed; offline fakes remain UNIT_VERIFICATION_ONLY with nativeAcceptance=false.","Preserve all 120 original test identities across tests/meta, tests/parity, tests/alpha1, tests/fixes/runner_boundary and tests/platform/linux_baseline. Add flat tests/live_backend discovery, run all six roots on every packet and prove no module/test omission, skip, xfail, deselection or test-only runtime shortcut.","Python 3.12.14 standard library and existing pinned conformance crypto/canonical helpers only; no new dependency, public API/signature schema/role, Makefile/dispatcher, PORTING ledger, warm-source, workflow or toolchain change. Kernel primitives and fixed operator prerequisites are preinstalled, never downloaded."]
deliverables: ["Implement a separate deterministic candidate builder; preserve ci/build_live_launcher.py bytes. Package only the allowlisted source/modules/public trust anchors and fixed recipes, sorted paths and fixed metadata. Reject symlinks, hardlinks, traversal, unknown extras, missing inputs, embedded credentials and mutable references.","Generate unsigned SOURCE_PACKAGE_ONLY inventory/SBOM-input manifests and reproducibility hashes from actual bytes. Do not invent Linux image digests or claim a Python archive is a Linux release. A --verify-reproducible mode builds twice under temporary output without host installation or downloads; tests invoke it inside declared offline acceptance.","Document the independent operator transaction: preexisting native host and cluster capacity, reviewed artifact/signature verification, root-owned install/custody and kernel prerequisites, fresh preflight evidence, exact versioned manifest, rollback retaining trust/replay history and no available-target fallback. Installation is manual external authority, not a product Make target or coding command.","Pin a complete candidate inventory covering supervisor, proxy server, fixed probes and integration hook. Final CONF-LIVE-006 rebuilds the candidate after its hook is present; publication of this source builder is not the final installed artifact."]
excluded: ["No administrator installation, provisioning, kernel/cluster policy modification on this workstation, live network/probe argv, emulation-based native PASS, paid API or third-party key, mutable artifact, warm-source access, external telemetry or source-to-runtime evidence promotion.","No edits to predecessor tests, crypto.py, canonical.py, models.py, schema.py, registry.py, campaign.py, live.py, cli.py, linux_readiness.py, existing schemas, ci/build_live_launcher.py, Makefile, dispatcher, toolchain, workflow or PORTING.yaml. New tests independently preserve old source guards; no relaxing immutable baseline hashes."]
prefetchCommands: []
offlineAcceptanceCommands: [["python3","-m","unittest","discover","-s","tests/meta","-p","test_*.py"],["python3","-m","unittest","discover","-s","tests/parity","-p","test_*.py"],["python3","-m","unittest","discover","-s","tests/alpha1","-p","test_*.py"],["python3","-m","unittest","discover","-s","tests/fixes/runner_boundary","-p","test_*.py"],["python3","-m","unittest","discover","-s","tests/platform/linux_baseline","-p","test_*.py"],["python3","-m","unittest","discover","-s","tests/live_backend","-p","test_*.py"],["make","campaign","CAMPAIGN=linux-baseline"],["make","evidence-verify","CAMPAIGN=linux-baseline"]]
offlineExecution: {"wrapperArgv":["./ci/verify-offline.sh"],"packetPathEnvironment":"HARNESS_TASK_PACKET","packetPathMode":"HASH_PINNED_READ_ONCE_NO_CHILD_PATH","commandTransport":"ARGV_ARRAY_V1","isolation":"OS_ENFORCED_DENY_ALL_OUTBOUND","sessionScope":"SINGLE_PROCESS_TREE","prefetchOutsideSession":false,"offlineEnvironment":{"UV_OFFLINE":"1","UV_FROZEN":"1","UV_NO_SYNC":"1"}}
expectedEvidence: ["Byte-for-byte reproducibility, exact package closure, altered source/public key, absent integration candidate, symlink/hardlink/traversal, archive metadata, secrets scan and rollback fail-closed custody checks.","All eight offline commands run in one signed deny-all process tree; all original 120 test identities and every predecessor backend test remain discovered and passing. New tests never replace real native qualification.","Source/CI/merge/exact-main and unsigned candidate/package evidence are separate from installed preflight, native artifacts, runtime, assurance and tenant acceptance. Missing independent backend/target/authority is NOT_RUN_ENV_UNAVAILABLE; no phase completion claim."]
rollback: "Revert this unconsumed source increment before successors begin. After consumption use a reviewed corrective packet. Never modify installation, tenant data, trust/replay history or historical evidence."
