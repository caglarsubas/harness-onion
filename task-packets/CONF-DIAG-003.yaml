id: "CONF-DIAG-003"
repository: "mas-harness-conformance-labs"
branch: "codex/conf-diag-003-completion-profiling"
objective: "One read-only whole1397-backend diagnostic of exact25fab12; per-case clocks and bounded stdlib function profiles; not product acceptance."
predecessors: ["MET-PERF-009","CONF-FIX-007"]
allowedPaths: ["tests/live_backend/test_proxy_server.py"]
warmSourceAccess: "PROHIBITED_DURING_IMPLEMENTATION"
sourceReuse: []
contracts: ["READ_ONLY_DIAGNOSTIC_REPLAY. allowedPaths is only the read-only discovery anchor. Whole135-file subject25fab12c168ff686e863291098fce0f3dba629bd/tree6f38052fb9a31e9fe8b9abb6b055ddd96f06854f is immutable; no overlay or product branch/PR. CONF-FIX-007 is the observed incomplete source-owner/history, not required successful acceptance.","Read architecture/completion-profiling.json and docs/alpha-2/COMPLETION_PROFILING.md. Activate only after all MET-PERF-009 source gates using the existing signed python-conformance-stdlib absolute trusted launcher and exact literal Python argv. No shell/command-string splitting, mutable code input or policy workaround.","All1397 original backend cases in normal discovery order. Three exact profiler-sensitive cases run with timing only and their inherited profiler behavior untouched; other1394 use cProfile. Never replace TestCase.run, any fixture, assertion, guard, admission or result classification.","Maximum1 diagnostic attempt,0 retries; durable reservation before activation binds signed request, META, subject, source inventory, toolchain and exact program. Failure/timeout/sleep consumes it. CONF-FIX-007 remains3/3 consumed; CI/main allowances are not substitutes.","Per-test timing includes observer overhead; cProfile function times are elapsed, not CPU. Current thread only, top20 rows per metric, overlapping cumulative totals, no missing-as-zero. An unfinished case has no complete timing/profile; preserve stack-only watchdog and original output."]
deliverables: ["Full retained stdout/stderr with progressive case identities, available wall/CPU/profile rows and stack-only timeout diagnostics; exact independent signed/source/toolchain custody.","Explicit profiler interference, top-N/clock/discovery/fixture/timeout limitations; no unprofiled speedup claim or automatic repair selection.","Read-only diagnostic evidence, even if all1397 pass; subsequent C1-C7 and full acceptance/release/native/tenant gates remain separate."]
excluded: ["Every product write/overlay/PR/merge, filtered/reordered/skipped tests, TestCase.run overrides, runtime monkeypatches, changed test result classification, extra command or dynamic code input, repeat/reset, time/guard/root/power/billing/warm-source changes and any acceptance promotion."]
prefetchCommands: []
offlineAcceptanceCommands: [["python3","-c","exec(\"\\\"\\\"\\\"CONF-DIAG-003 observer. No acceptance, source edits or test substitutions.\\\"\\\"\\\"\\nimport cProfile\\nimport faulthandler\\nimport hashlib\\nimport json\\nfrom pathlib import Path\\nimport sys\\nimport sysconfig\\nimport time\\nimport unittest\\nEXPECTED_IDS = '03ab92921cd44154d4658e7e0205ac6134a66cf905ab0baf360f52dc01369a94'\\nROOT = Path.cwd()\\nSTDLIB = Path(sysconfig.get_path('stdlib'))\\nTIMING_ONLY = {\\n 'test_supervisor.PerformanceArithmeticTests.test_fixed_three_sample_workload',\\n 'test_supervisor.PerformanceSourceProofTests.test_benchmark_restores_observer_after_workload_exception',\\n 'test_supervisor.PerformanceSourceProofTests.test_benchmark_refuses_ambient_observer_without_replacing_it',\\n}\\ndef emit(event, **fields):\\n print(json.dumps(dict(event=event, evidenceClass='WORKLOAD_DIAGNOSTIC_ONLY',\\n      **fields), sort_keys=True, allow_nan=False), flush=True)\\ndef label(code):\\n if isinstance(code, str):\\n  return ['BUILTIN', 0, code[:160]]\\n path = Path(code.co_filename)\\n for root, tag in ((ROOT, 'SUBJECT'), (STDLIB, 'STDLIB')):\\n  try:\\n   return [tag + '/' + str(path.relative_to(root)), code.co_firstlineno, code.co_name]\\n  except ValueError:\\n   pass\\n return ['OTHER', code.co_firstlineno, code.co_name]\\nclass Result(unittest.TextTestResult):\\n def startTest(self, test):\\n  super().startTest(test)\\n  self.case_id = test.id()\\n  emit('case-start', testId=self.case_id)\\n  faulthandler.dump_traceback_later(30, repeat=True, exit=False)\\n  self.wall = time.perf_counter()\\n  self.cpu = time.thread_time()\\n  self.process_cpu = time.process_time()\\n  self.profiler = None\\n  if self.case_id not in TIMING_ONLY:\\n   if sys.getprofile() is not None:\\n    raise RuntimeError('unexpected ambient profiler')\\n   self.profiler = cProfile.Profile()\\n   self.profiler.enable()\\n def stopTest(self, test):\\n  cpu = time.thread_time() - self.cpu\\n  wall = time.perf_counter() - self.wall\\n  process_cpu = time.process_time() - self.process_cpu\\n  intact = sys.getprofile() is self.profiler if self.profiler else None\\n  if self.profiler:\\n   self.profiler.disable()\\n  rows = self.profiler.getstats() if self.profiler else []\\n  def top(key):\\n   return [dict(function=label(e.code), calls=e.callcount,\\n      recursiveCalls=e.reccallcount, selfElapsed=e.inlinetime,\\n      cumulativeElapsed=e.totaltime)\\n     for e in sorted(rows, key=key, reverse=True)[:20]]\\n  emit('case-finish', testId=self.case_id, wallSeconds=wall,\\n   threadCpuSeconds=cpu, processCpuSeconds=process_cpu,\\n   profilerActiveAtFinish=intact, functionCount=len(rows),\\n   topSelf=top(lambda e: e.inlinetime),\\n   topCumulative=top(lambda e: e.totaltime))\\n  faulthandler.cancel_dump_traceback_later()\\n  super().stopTest(test)\\ndef identities(suite):\\n for item in suite:\\n  if isinstance(item, unittest.TestSuite):\\n   yield from identities(item)\\n  else:\\n   yield item.id()\\ndef main():\\n faulthandler.dump_traceback_later(30, repeat=True, exit=False)\\n try:\\n  wall, cpu = time.perf_counter(), time.process_time()\\n  suite = unittest.defaultTestLoader.discover('tests/live_backend', 'test_*.py')\\n  ids = sorted(identities(suite))\\n  digest = hashlib.sha256(json.dumps(ids, separators=(',', ':')).encode()).hexdigest()\\n  if len(ids) != 1397 or len(set(ids)) != 1397 or digest != EXPECTED_IDS:\\n   raise ValueError('exact whole-backend inventory required')\\n  emit('discovered', cases=len(ids), idsSha256=digest,\\n   wallSeconds=time.perf_counter()-wall, processCpuSeconds=time.process_time()-cpu)\\n  result = unittest.TextTestRunner(verbosity=2, resultclass=Result).run(suite)\\n  emit('finished', cases=result.testsRun, failures=len(result.failures),\\n   errors=len(result.errors), skips=len(result.skipped))\\n  return 0 if result.wasSuccessful() and not result.skipped and result.testsRun == 1397 else 1\\n finally:\\n  faulthandler.cancel_dump_traceback_later()\\nif __name__ == '__main__':\\n raise SystemExit(main())\\n\")"]]
offlineExecution: {"wrapperArgv":["./ci/verify-offline.sh"],"packetPathEnvironment":"HARNESS_TASK_PACKET","packetPathMode":"HASH_PINNED_READ_ONCE_NO_CHILD_PATH","commandTransport":"ARGV_ARRAY_V1","isolation":"OS_ENFORCED_DENY_ALL_OUTBOUND","sessionScope":"SINGLE_PROCESS_TREE","prefetchOutsideSession":false,"offlineEnvironment":{"UV_OFFLINE":"1","UV_FROZEN":"1","UV_NO_SYNC":"1"}}
expectedEvidence: ["WORKLOAD_DIAGNOSTIC_ONLY;1 attempt0 retries; all1397 expected,1394 function profiles plus3 timing-only cases. Partial results remain incomplete and cannot be pooled.","No full8-command acceptance, C7, CI, merge, exact-main, artifact, native or tenant claim. A new repair requires separate reviewed authority; Alpha2 ONGOING."]
rollback: "No source mutation or merge to revert. Retire only exact diagnostic slot; retain all failed/completed logs and signed custody. Do not erase trust, nonce, source or tenant history."
