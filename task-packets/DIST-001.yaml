id: DIST-001
repository: mas-harness-distribution
branch: codex/dist-001-bootstrap-tools
objective: Bootstrap the offline bundle CLI, locked toolchain/prefetch, schema validation, local OCI fixture, and no-network harness.
predecessors: [CON-005, MET-003]
allowedPaths: [".github/workflows/verify.yml", "cmd/harness-bundlectl/", "src/planeon_distribution/", "schemas/", "tests/bootstrap/", "fixtures/oci-layout/", "toolchain.lock", "pyproject.toml", "uv.lock", "Makefile", "README.md", "AGENTS.md", "CONTRIBUTING.md", ".gitignore", "LICENSE", "NOTICE", "SECURITY.md", "PORTING.yaml", "ci/"]
warmSourceAccess: PROHIBITED_DURING_IMPLEMENTATION
sourceReuse:
  - {repository: "git@github.com:caglarsubas/data-source-harness.git", commit: "858281f4b845ffacfe05cdb2c40a402c237d4c54", reuseMode: DISCOVERY_ONLY, paths: ["deployment/profiles/","scripts/"], strategy: "historical reference-only discovery provenance; implementation access and recursive copying forbidden"}
  - {repository: "git@github.com:caglarsubas/data-source-harness.git", commit: "858281f4b845ffacfe05cdb2c40a402c237d4c54", reuseMode: REFERENCE_ONLY, paths: ["src/data_source_harness/packaging.py","deployment/profiles/air-gapped.json","deployment/profiles/local-laptop.json","deployment/profiles/self-hosted.json","scripts/build-airgap-wheelhouse.sh","scripts/check-local-only.py"], strategy: "historical reference-only exact-blob provenance for independent clean-room parity; implementation access, copying, adaptation, translation, and derivative reuse are forbidden; direct reuse requires a future packet revision after porting admission is enabled"}
contracts: ["Consumes profile, BOM, bundle, and release schemas v1alpha1", "Produces harness-bundlectl shell and OCI fixture contract v1", "Produces closed packet-local Make target descriptor dispatch v1", "Produces inert destination PORTING ledger v1 with zero current copy authorizations"]
deliverables: ["Frozen offline tools, validation CLI, local OCI-layout fixture, deterministic filesystem helpers, no-network test harness, and common Make targets.", "Bootstrap Makefile delegates only to ci/run_make_target.py, which validates closed ci/targets/<packet-id>.json descriptors, exact target and variable bindings, cumulative lexical handler order, and direct argv without shell strings; later packets never edit Makefile.", "PORTING.yaml contains only the closed NO_AUTHORIZATION bootstrap sentinel and cannot name a source path, mapping, copied file, or applied port."]
excluded: ["Remote registry, real bundle closure, SBOM/scan, signing, or Helm profiles."]
prefetchCommands: [["make","prefetch"]]
offlineAcceptanceCommands: [["make","fixture-verify"],["make","zero-bill"]]
offlineExecution: {"wrapperArgv":["./ci/verify-offline.sh"],"packetPathEnvironment":"HARNESS_TASK_PACKET","packetPathMode":"HASH_PINNED_READ_ONCE_NO_CHILD_PATH","commandTransport":"ARGV_ARRAY_V1","isolation":"OS_ENFORCED_DENY_ALL_OUTBOUND","sessionScope":"SINGLE_PROCESS_TREE","prefetchOutsideSession":false,"offlineEnvironment":{"UV_OFFLINE":"1","UV_FROZEN":"1","UV_NO_SYNC":"1"}}
expectedEvidence: ["Toolchain digests and offline CLI report.", "Valid/invalid OCI fixture vectors and no network trace.", "Negative Make-dispatch vectors reject missing, unknown, or duplicate targets; owner or filename mismatch; undeclared variables; shell transport; and zero or ambiguous applicable handlers.", "PORTING bootstrap validation proves zero authorized source mappings and rejects any copy claim without a future approved PORT_CANDIDATE packet."]
rollback: Revert the unconsumed distribution scaffold and fixture.
