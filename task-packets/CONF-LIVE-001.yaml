id: "CONF-LIVE-001"
repository: "mas-harness-conformance-labs"
branch: "codex/conf-live-001-session-contracts"
objective: "Session contracts and cumulative inventory under the approved trusted backend roadmap; source coding only."
predecessors: ["MET-LIVE-001","CONF-LINUX-001","CON-MODEL-001"]
allowedPaths: ["src/harness_conformance/live_session.py","src/harness_conformance/live_backend_authority.py","schemas/v1alpha1/live-backend-session.schema.json","fixtures/live-backend/session-vectors.json","fixtures/live-backend/baseline.json","tests/live_backend/_fixtures.py","tests/live_backend/_inventory.py","tests/live_backend/test_session.py","tests/live_backend/test_inventory.py","docs/live-backend/session.md"]
warmSourceAccess: "PROHIBITED_DURING_IMPLEMENTATION"
sourceReuse: []
contracts: ["Consumes docs/alpha-2/LIVE_BACKEND_READINESS.md and architecture/live-backend-roadmap.json from the exact merged MET-LIVE-001 authority, the existing trusted live-runner contract and unchanged CONF-LINUX-001/CON-007 wire contracts. Pin exact merged predecessor SHA and complete source inventory before edits.","Source-only enablement before the native gate is limited to these six packets. No installation or live execution occurs in a coding run. Missing authority, supported OS backend or native capacity fails closed; offline fakes remain UNIT_VERIFICATION_ONLY with nativeAcceptance=false.","Preserve all 120 original test identities across tests/meta, tests/parity, tests/alpha1, tests/fixes/runner_boundary and tests/platform/linux_baseline. Add flat tests/live_backend discovery, run all six roots on every packet and prove no module/test omission, skip, xfail, deselection or test-only runtime shortcut.","Python 3.12.14 standard library and existing pinned conformance crypto/canonical helpers only; no new dependency, public API/signature schema/role, Makefile/dispatcher, PORTING ledger, warm-source, workflow or toolchain change. Kernel primitives and fixed operator prerequisites are preinstalled, never downloaded."]
deliverables: ["Implement closed internal session/request/receipt validation and pure state transition functions under the exact interface and state table in the approved guide. Preserve existing public envelope/capacity/Linux evidence formats; test canonical bytes, bounded sizes, expiry, duplicate fields, wrong nonce/scope/digests and every invalid transition.","Seed baseline.json from the clean exact conformance source commit: 120 existing test IDs, every tracked path/hash and the original five-suite collection inventory. Pin authority and source-only model release manifest digests. All baseline data is non-executable; immutable test and helper files retain bytes.","Implement additive inventory tests checking AST and actual unittest discovery for all six roots and the pinned predecessor identities. Allow later production changes only at the exact new packet paths and the final live_launcher.py hook; never amend old hash guard exceptions.","Document that serialized session objects are not authority. API shape validate_session(value, expected_binding, now) returns validated data only; no token/env/header/verified flag unlocks execution. Protected supervisor ownership, channel peer/process binding and one-use transitions are prerequisites supplied by later packets.","Implement a new pure authority adapter for CONF-LIVE-006 and its exact eight-command set. Do not call or monkeypatch verify_linux_authority, which is permanently pinned to CONF-LINUX-001/seven commands. Reuse existing canonical/crypto/capacity validation primitives, independently cover every legacy signature, purpose, validity/revocation, scope, command/envelope/release/endpoint binding and capacity check, and keep the legacy verifier unchanged. Pin the new packet digest from the approved meta packet, not caller input."]
excluded: ["No administrator installation, provisioning, kernel/cluster policy modification on this workstation, live network/probe argv, emulation-based native PASS, paid API or third-party key, mutable artifact, warm-source access, external telemetry or source-to-runtime evidence promotion.","No edits to predecessor tests, crypto.py, canonical.py, models.py, schema.py, registry.py, campaign.py, live.py, cli.py, linux_readiness.py, existing schemas, ci/build_live_launcher.py, Makefile, dispatcher, toolchain, workflow or PORTING.yaml. New tests independently preserve old source guards; no relaxing immutable baseline hashes."]
prefetchCommands: []
offlineAcceptanceCommands: [["python3","-m","unittest","discover","-s","tests/meta","-p","test_*.py"],["python3","-m","unittest","discover","-s","tests/parity","-p","test_*.py"],["python3","-m","unittest","discover","-s","tests/alpha1","-p","test_*.py"],["python3","-m","unittest","discover","-s","tests/fixes/runner_boundary","-p","test_*.py"],["python3","-m","unittest","discover","-s","tests/platform/linux_baseline","-p","test_*.py"],["python3","-m","unittest","discover","-s","tests/live_backend","-p","test_*.py"],["make","campaign","CAMPAIGN=linux-baseline"],["make","evidence-verify","CAMPAIGN=linux-baseline"]]
offlineExecution: {"wrapperArgv":["./ci/verify-offline.sh"],"packetPathEnvironment":"HARNESS_TASK_PACKET","packetPathMode":"HASH_PINNED_READ_ONCE_NO_CHILD_PATH","commandTransport":"ARGV_ARRAY_V1","isolation":"OS_ENFORCED_DENY_ALL_OUTBOUND","sessionScope":"SINGLE_PROCESS_TREE","prefetchOutsideSession":false,"offlineEnvironment":{"UV_OFFLINE":"1","UV_FROZEN":"1","UV_NO_SYNC":"1"}}
expectedEvidence: ["Closed-schema positives plus per-field/type/extra/missing/depth/duplicate/nonfinite/expiry mutations; transitions and all 120 predecessor identities; forged session data must not authorize any operation.","All eight offline commands run in one signed deny-all process tree; all original 120 test identities and every predecessor backend test remain discovered and passing. New tests never replace real native qualification.","Source/CI/merge/exact-main and unsigned candidate/package evidence are separate from installed preflight, native artifacts, runtime, assurance and tenant acceptance. Missing independent backend/target/authority is NOT_RUN_ENV_UNAVAILABLE; no phase completion claim."]
rollback: "Revert this unconsumed source increment before successors begin. After consumption use a reviewed corrective packet. Never modify installation, tenant data, trust/replay history or historical evidence."
